#include <cmath>
#include "black.h"

double BlackScholes(double notional, double strike, double expiry, double spot, double vol, double rate, bool isCall)
{
	/*
	implement the black formula to compute the call optoin and put optoin price
	*/
	return 0;
};

